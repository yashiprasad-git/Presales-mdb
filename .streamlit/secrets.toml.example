# Copy to secrets.toml (do not commit secrets.toml).
# MDB updater + monitor dashboard only — no OpenAI key required.

DATABASE_URL = "postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
MONDAY_API_KEY = "your_monday_api_token"
